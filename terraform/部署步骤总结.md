# CloudLens 部署步骤总结

## 🎯 最简单的3步部署

### 第1步：申请 AWS 密钥（5分钟）

1. 打开: https://console.aws.amazon.com
2. 搜索 "IAM" → 进入 IAM 服务
3. 用户 → 您的用户名 → 安全凭证
4. 创建访问密钥 → 保存 Access Key ID 和 Secret Access Key
5. 运行: `aws configure` 输入密钥

**详细步骤**: 查看 [简单部署指南.md](./简单部署指南.md) 第一步

---

### 第2步：填写配置（2分钟）

```bash
cd terraform
cp terraform.tfvars.example terraform.tfvars
nano terraform.tfvars
```

**只需要改3个地方**:
1. `aws_region = "ap-northeast-1"` (日本) 或 `"ap-east-1"` (香港)
2. `ssh_public_key = "ssh-rsa ..."` (运行 `cat ~/.ssh/id_rsa.pub` 获取)
3. `mysql_password = "强密码"` (至少16位)

---

### 第3步：一键部署（10分钟）

```bash
# 方法1：使用一键脚本（推荐）
./一键部署.sh

# 方法2：手动部署
terraform init
terraform apply
```

等待完成，访问: `https://cloudlens.songqipeng.com`

---

## 📚 文档说明

- **快速开始.txt** - 纯文本，最简单
- **README_简单版.md** - 超简单版（推荐新手）
- **简单部署指南.md** - 详细步骤（推荐）
- **README.md** - 完整文档（高级用户）

---

## 🌏 区域选择

- **日本 (ap-northeast-1)**: 推荐，¥7,500/月
- **香港 (ap-east-1)**: 延迟最低，¥8,400/月

---

*最后更新: 2026-01-23*
